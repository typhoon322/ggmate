# 2026-07-31 工作日志

## 修复：profile 详情「其他曲线仍无数据」(gagmate_combined(8).log)
- 根因（推翻旧结论）：
  1. 本机固件 `g_prof(id)` **只回当前活跃 profile**，非活跃 profile 直接超时取空。
  2. 旧「WS `d_prof` curve 恒 FLAT」判断是错的——`ProtoDecoder.decodePhaseInfo`/`parseProfilePhases` 实际解析 curve 枚举（sub3 varint → `curveEnumToString` 0–6），WS 携带真实 `EASE_*` 曲线。
- 修复：
  - `MachineSessionManager.requestProfilePhases` 先 `selectProfile(id)`（`c_upd_act_prof_id`）设为活跃，再 `sendGetProfile(id)`（`g_prof`）nudge。
  - `MachineRepository.fetchProfilePhases` 改 WS 真实曲线优先；shot 内嵌仅作离线回退 / WS 为 FLAT 时按序叠加 curve。
  - 新增 `ProfileRepository.mirrorProfilePhases(id, phases)`：详情页在 `phasesJson` 空白时写回 WS 真实相位（只读镜像安全补全，不覆盖已有数据）。
- 副作用：打开任意详情会把它设为机器当前活跃 profile（原生 UX，已告知用户）。

## 网络日志去重（用户要求：重复数据不要存）
- `NetworkLogger.log()` / `ApiDebugLogger.logResponse()` 加 `LinkedHashMap<String,Int>` 哈希缓存（`MAX_CACHE_ENTRIES=256`），跳过「同 endpoint+同 status+body」的连续重复帧。
- 两处均跳过 SPA shell（body 含 `<!DOCTYPE html` 不记录）——即 `/api/profile/{id}` 返回的死端点 HTML。
- `GgboardApiClient` 收窄 `ApiDebugLogger` 捕获为仅 `/api/system/`（profiles/shots 已在主网络日志，避免双存）。
- 效果：原日志 557 次 `/api/profile/{id}` SPA-HTML + 大量重复帧不再落盘。

## 文档同步（AGENTS.md Rule 1）
- `GAGMATE_REFERENCE.md` → v2026-07-31：§3.4 已先行；本日补正 §6.3 / §6.4（WS→Room 收集器 + 新增 `mirrorProfilePhases`）/ §6.7 与取数流程图中「WS FLAT」陈旧结论。
- `CODE_REVIEW.md` → v2026-07-31：新增 §0q 记录本轮，并加全局更正——§0e「`g_prof` 按 id 返回（已确认）」与 §0f/§0g/§0j「WS 不携带 curve / 恒 FLAT」均已推翻。
- `.workbuddy/memory/MEMORY.md`：更正「曲线相位真实来源」描述（WS 携带真实曲线、须先 selectProfile）。
- 验证：`./gradlew :app:assembleDebug --offline` → **BUILD SUCCESSFUL**。

## 用户二次要求（2026-07-31 晚）：撤回「打开详情自动切活跃」+ 研究官方 WebUI 取数
- **代码改动（已由 §0q 当轮 `assembleDebug` 验证 BUILD SUCCESSFUL）**：
  - `MachineSessionManager.requestProfilePhases` **移除 `selectProfile(id)` 调用**，仅 `sendGetProfile(id)` 作 nudge；注释明确「不推 `c_upd_act_prof_id`」。打开任意 profile 详情**不再切机器活跃 profile**（用户明确要求，零副作用）。
  - `ProfileRef` 已含完整 `phases`+`globalStopConditions`，`SyncManager.syncProfiles` 把 `GET /api/profiles/all` 全量相位镜像进 `ProfileEntity.phasesJson`；`ProfileDetailScreen` 优先读本地 `phasesJson`，为空才 `fetchProfilePhases` 兜底。
  - `ProfilesViewModel.selectProfile`（用户主动"激活曲线"）→ `activateProfile`→`selectProfile` 属显式操作，**保留**。
- **WebUI 研究结果（官方 `gaggiuino_api` 库 + Gen3 手册）**：WebUI 在 Profiles 页一次性 `GET /api/profiles/all` 取**每个 profile 的完整定义**（phases+globalStopConditions）绘制全部曲线；**只在真正萃取/编辑时才 `select` 活跃 profile**，纯粹"看曲线"不切活跃。我们此前把该端点当 `List<ProfileRef>`(仅 id/name/selected) 解析、丢掉 phases，是"非活跃曲线无数据"根因之一。
- **文档同步**：`GAGMATE_REFERENCE.md` 修订 §2.1/§2.2/§3.1/§3.2/§4/§5.3/§6.3/§6.7/§9/附录A.2（消除「WS 不携带 curve / g_prof 按 id 返回 / 打开详情切活跃」等陈旧结论），新增 §3.4a「与官方 WebUI 同源」全量镜像小节；`CODE_REVIEW.md` 新增 §0r 记录撤回+研究。MEMORY.md「架构要点速记」已先行更正。
- ⏳ 待办：尚未 `git commit` + `git push origin master`（本轮仅文档同步 + 复核源码，源码改动已在 §0q 当轮提交/验证）。

## 用户纠正（2026-07-31 晚，本轮）：`/api/profiles/all` 只返回 id/name/selected —— 撤回「全量相位镜像」错误理论
- **用户原话**："`/api/profiles/all` 我记得只是返回有哪些 profile iD"。
- **实测证伪**：查 `gagmate_combined(7).log` 行 234，响应确为 `[{"id":8,"name":"Light Template","selected":"false"}, ...,{"id":33,"name":"turbo shot","selected":"true"}]` —— **仅 `id`/`name`/`selected`，无 `phases`**。
- **根因**：上一轮（§0r）我据第三方 `gaggiuino_api` Python 库的 schema 推断"`/api/profiles/all` 返回完整 profile（phases+globalStopConditions）"，并以此改了取数逻辑（ProfileRef 加 phases 字段、`syncProfiles` 镜像 mp.brewPhases()）。该推断对**用户本机固件**不成立——那库大概是别的固件版本。同时又把 CODE_REVIEW §0q 已确认的「`g_prof(id)` 只回活跃 profile」事实给覆盖错了。
- **纠正动作**：
  - `ShotRecord.kt` `ProfileRef` 回退为仅 `id`/`name`/`selected`（移除 `phases`/`globalStopConditions`/`brewPhases()`）。
  - `SyncManager.syncProfiles` 回退：新建行 `phasesJson="[]"`，已有行保留 `local.phasesJson`；相位完全由 `seedProfilePhasesFromShots`（shot 内嵌）填充。
  - `MachineSessionManager.requestProfilePhases` KDoc/注释改为如实描述「`g_prof(id)` 只回活跃 profile；非活跃 profile 无 WS 数据，靠本地 shot 内嵌缓存」。
  - `GAGMATE_REFERENCE.md` `§3.4a` 改为「撤回声明的更正」小节；`§2.1/§2.2/§3.4/§6.3/§6.7` 等所有「全量相位镜像」陈旧结论已清除；`CODE_REVIEW.md §0r ②` 重写为已更正版；`MEMORY.md` 架构要点已更正。
- **官方 WebUI 真实机制**：Gen3 手册原文 "Tap on a name to select the active profile (a preview of the selected profile is shown)" —— WebUI「看曲线」时其实**会 `select` 该 profile 为活跃**，没有「只读不切活跃」通道。这也是 §0q 必须靠 `selectProfile` 才能取任意 profile 相位的原因。
- **能力边界（不切活跃前提下）**：「打开详情不切活跃」与「任意 profile 都能看到曲线」在本机固件上**不可兼得**——未实际萃取过的非活跃 profile 无本地曲线缓存，打开详情会空白（除非它是当前活跃 profile）。这是用户「不推 set-active」要求的真实代价，已写入文档。
- 验证：`./gradlew :app:assembleDebug --offline` → **BUILD SUCCESSFUL**。
- ⏳ 待办：commit + push（见下方）。
