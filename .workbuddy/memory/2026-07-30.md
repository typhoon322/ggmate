# 2026-07-30

## 曲线详情：turbo shot 阶段 2/3（time=0）画不出 → 已修复
- 根因：`PhaseV3.toBrewPhase()` 只从 `target?.time` 取阶段时长；`turbo shot`(id 33) 阶段 2/3 无 `target.time` 且 `stopConditions` 为空（全局 weight:100 停止）→ `time` 塌缩 0.1f；`generateProfileChartPoints` 仅 `coerceAtLeast(0.1f)` → 零宽竖线看不见。CORE Turbo 各阶段 time 齐全故正常。
- 修复：
  - `PhaseModels.toBrewPhase()` 重写：时长优先级 `target.time` → FLOW 阶段 `waterPumpedInPhase/flow` 估算 → `stopConditions.time` → 兜底 `MIN_PHASE_SECONDS=4s`；停止条件写入 `BrewPhase.condition`/`value`。
  - `CurveChart.generateProfileChartPoints()` 绘制时长下限 0.1f→`MIN_PHASE_DRAW_SECONDS=4f`（旧库 0.1s 行也能立即显示，无需重新同步）。
  - `ProfileDetailScreen.toPhaseV3()` 用 condition/value 重建 stopConditions；`PhaseCard` 新增 weight 停止条件展示。
- 影响：`ProfileDetailScreen` 与 `DashboardScreen`「当前曲线」共用生成器，两处一并修复。BUILD SUCCESSFUL。
- 文档：GAGMATE_REFERENCE §格式转换(toBrewPhase)/§7.2(generateProfileChartPoints)、CODE_REVIEW §0n，日期升 2026-07-30。

## 流量估算替代 4s 占位（用户纠正：别用占位，用流量估算）
- 根因：`EmbeddedProfile.globalStopConditions.weight`(turbo shot=100) 一直没传给 `toBrewPhase()`，导致按重量停止、无显式 time 的 FLOW 阶段只能落到 4s 占位。
- 新增 `PhaseModels.List<PhaseV3>.toBrewPhases(global)`：`time = 剩余体积(weight − 已知体积) / Σ(flow)`，同组阶段时长相等、体积按流量比例分摊。turbo shot → P2/P3 各 11s（总 27s）。
- `ShotRecord.toShotRecord()` 现传 `profile.globalStopConditions` → 今后新 shot 直接算真实时长。
- 存量自修复：`SyncManager.repairVolumeDrivenPhaseTimes()`（AppContainer.init appScope 启动一次，幂等），用 shot.volume 作代理体积重算并回写 embedded_phases_json + 重新 seedProfilesFromAllShots()。
- 文档：GAGMATE_REFERENCE §3.4/§5.3、CODE_REVIEW §0p。BUILD SUCCESSFUL。commit 0455d1d。
