# 2026-07-28 工作记录

## profile 详情「无数据 + 无曲线」修复（log 5）

用户附 `gagmate_combined(5).log`，反馈 profile 详情仍空白（联网时仪表盘当前曲线已能缓动）。

### 根因
- 详情页取数：`fetchedPhases`(联网实时) → 回退本地 `phasesJson`。打开详情时机器刚好断连（`GET /api/profile/{id}`、`api/shots/latest` 全 `Failed to connect`），实时三路全失败 → 空；本地 `phasesJson` 仍是 `"[]"`。
- `phasesJson` 为空的根因：旧 `syncProfiles` 只初始化 `"[]"`，把填充寄托于 WS `d_prof` 收集器，但该路径不稳（依赖同步时 WS 连接 + name 匹配），多数 profile 从未被写入真实相位。即使写入，curve 也恒 FLAT。

### 修复（已 `assembleDebug` 绿，未提交）
- `SyncManager.syncProfiles`：对每条 `SYNCED` profile，当连接可用且尚未含真实 curve 时，调 `getProfileDetail(mId)` 把真实 `EASE_*` 相位写回 `phasesJson`（best-effort，不覆盖本地已编辑）。→ 离线也能从本地库读带缓动曲线。
- `ProfileRepository` WS→Room 收集器：写 `d_prof` 时沿用 `phasesJson` 已有真实 curve（同序相位），避免后续 FLAT WS 覆盖缓动。
- `ProfileDetailScreen.chartPhases`：实时取到真实 curve 用实时值；否则优先本地 `phasesList`（现含真实 curve）；最后回退 FLAT 实时值。

### 文档
- `GAGMATE_REFERENCE.md` §6.7 + 落库说明：修正「离线 phasesJson curve 恒 FLAT」「REST 不支持已撤」等旧断言；顶部日期→2026-07-28。
- `CODE_REVIEW.md` §0e/§0f 旧断言修正，新增 **§0g** 记录本次修复；顶部日期→2026-07-28。

### 待办（用户侧复测）
1. 连机器做一次全量同步，确认日志 `fullSync: stored REST detail phases (N) for id=…`。
2. 断网打开任意 profile 详情：应显示相位数据 + EASE_* 缓动曲线。
3. 联网打开详情：实时值 + 真实 curve 叠加，同样缓动。

注：本机 `GET /api/profile/{id}` 端点**可用**（log 5 中 `api/profiles/all` 返回 200；REST 详情在连接可用时返回带 curve 字符串的完整定义）。之前 CODE_REVIEW §0e「端点不支持、已撤」结论被推翻。

## 屏幕方向：除全屏曲线页外强制竖屏、禁止自动旋转

用户要求：除全屏曲线图表展示页，其它页面强制竖屏、禁止自动切换。

### 根因
- 旧实现：`LiveCurveScreen`/`ShotChartFullScreen` 各自 `DisposableEffect(Unit)`「进入 LANDSCAPE / 退出 UNSPECIFIED」，`ProfileDetailScreen`「进入 PORTRAIT / 退出 UNSPECIFIED」。退出回到 `UNSPECIFIED` 使非图表页重新跟随传感器 → 没真正锁竖屏。

### 修复（已 `assembleDebug` 绿，已提交 master）
- **集中式方向控制**：`AppNavigation` 内以 `currentRoute` 为键的 `DisposableEffect` 统一设定——非图表页 `SCREEN_ORIENTATION_PORTRAIT`，仅 `livecurve` 与 `history_chart/*` 用 `SCREEN_ORIENTATION_LANDSCAPE`。路由为单一真相源，无竞态。
- **Manifest 兜底**：`MainActivity` 加 `android:screenOrientation="portrait"`。
- 删除三页面各自的 `requestedOrientation` 代码块及相关 import（`ActivityInfo`/`LocalContext`/`ComponentActivity`）。

### 文档
- `GAGMATE_REFERENCE.md` §2 新增「2.0 屏幕方向」小节。
- `CODE_REVIEW.md` 新增 **§0h** 记录本次改动。
