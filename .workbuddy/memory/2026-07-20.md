# 2026-07-20

## Code review of GagMate Android app (Gaggiuino espresso machine controller)
- Scanned full `app/src/main` (54 kt files, ~7k LOC). Wrote review to `/Users/yanx/Documents/GagMate/CODE_REVIEW.md`.
- **Critical:** `AppContainer.init()` reads `machineSession` (lateinit) as an arg to `SyncManager(...)` before assigning it → `UninitializedPropertyAccessException` at app launch. Fix: create `machineSession` before `syncManager`.
- **High:** `GaggiuinoV3Client.kt` (337 LOC) is dead code that duplicates `MachineSessionManager`/`ProtoDecoder`; its `handleDActProf`/`updateActiveProfile` are broken. WS host never refreshes after Settings change (only REST URL updates).
- **Medium:** `SyncManager.syncShots()` loops `1..latestShotId` with one sequential HTTP call per shot; `GgboardApiClient` debug log uses escaped `$`; WS host extraction only strips `http://`; `onClosed` doesn't reconnect; reconnect leaks OkHttpClient; `getConnectionUrl()` returns hardcoded default.
- Architecture is otherwise clean (Room + Retrofit + WS + Compose, Flow-based repos, CrashLogger). User only asked for review, not fixes.

## Deep re-review after reading GAGMATE_REFERENCE.md (user: "由你继续开发")
- Re-read EVERY layer (UI screens, ViewModels, repos, protocol, api, manifest). **Important: many prior findings were ALREADY FIXED in-tree in the last 24h** — the `AppContainer.init()` crash, WS `restart()` after Settings change, `onClosed` reconnect, OkHttpClient reuse, and the escaped-`$` log bug are all resolved now.
- **NEW high bugs found (verified, not yet fixed):**
  - NEW-A: Profile detail auto-fetch (g_prof) results never display — dialog holds stale `selectedProfile` and `loadingPhases` flips to false instantly.
  - NEW-B: Dashboard `flush()`/`startBrew()`/`stopBrew()`/`toggleSteam()`/`primePump()` overload `_error` as a generic message channel → any successful action triggers the full-screen "Cannot connect" takeover (screen shows error layout when `error != null`).
  - NEW-C: Start/Stop Brew, Steam, Prime are non-functional stubs (set `_error` only, send no WS command). FAB does nothing.
  - NEW-D: Steam-temp & flow-rate gauges hardcoded 0f; brew info cards hardcoded "--"/0.
  - NEW-E: `syncProfiles` reports fake `profilesUploaded++` without calling `uploadProfile` (comment wrongly says REST unavailable; `saveEditedProfile` uses it).
  - NEW-F: `syncShots` still loops 1..latestId one HTTP each.
  - NEW-G: `MainActivity` calls `machineSession.start(appScope)` without Context → connection-success sound never plays (appContext null).
  - NEW-H: host parsing inconsistent (start strips https+http; restart/saveAndApply strip only http).
  - NEW-I/J/K/L + security (usesCleartextTraffic=true overrides domain-config) + O8 (shots count dropped) — see CODE_REVIEW.md.
- Consolidated everything (fixed-status table + new bugs + optimization debt + fix order) into updated `CODE_REVIEW.md`.
- User explicitly said "先整理架构、查 bug"，so delivered analysis only; next step is fixing priority 1 (NEW-B+C), 2 (NEW-A), 3 (NEW-E/F/L).

## Fixes applied (session 2): priorities 1-3 + WS global singleton — DONE, assembledDebug passes
- **WS singleton hardened** (`MachineSessionManager`): added `ConnectionState.RECONNECTING`; exponential-backoff auto-reconnect with give-up-after-6-attempts → terminal `ERROR`; `start(appScope, context)` now requires Context (sound restored); `normalizeHost()` for consistent http/https stripping; single OkHttpClient reuse; `startBrew()`/`stopBrew()` real opmode commands. `ConnectionState.kt` gained RECONNECTING.
- **NEW-B**: DashboardViewModel got separate `_message` channel; control success no longer pollutes `_error`; only terminal ERROR shows full takeover. Screen shows reconnecting banner + message Snackbar.
- **NEW-C**: flush/tare/setpoint/weight/startBrew/stopBrew send real WS commands; steam/prime emit honest info messages.
- **NEW-A**: added `ProfileDao.observeById` → `LocalDataRepository.observeProfile` → `ProfilesViewModel.profileFlow`; `ProfilesScreen` now drives dialog from live `liveProfile` flow so WS-returned phases display. Also fixed paste-dialog double-set and delete-by-wrong-id.
- **NEW-E**: `syncProfiles()` now actually calls `uploadProfile()` for LOCAL_ONLY/MODIFIED and counts real uploads.
- **NEW-F**: `syncShots()` skips already-stored IDs and fetches missing ones with bounded parallelism (`Dispatchers.IO.limitedParallelism(4)`).
- **NEW-L**: Context passed to start() (sound); manifest `usesCleartextTraffic` removed, single `base-config cleartextTrafficPermitted=true` in network_security_config; noisy logs guarded by `BuildConfig.DEBUG` (enabled `buildFeatures.buildConfig=true` in app/build.gradle.kts).
- **NOT changed**: Room `fallbackToDestructiveMigration` (needs schema knowledge); NEW-D hardcoded gauges; dead `GaggiuinoV3Client.kt` (priority 4-6).
- Build verified: `./gradlew :app:assembleDebug --offline` succeeds.
- All fixes documented in CODE_REVIEW.md §0.

## 改动：标题栏白边 + 离线弹错误（session 4 续2）
- **白边根因**：`GagMateTheme` 默认跟随系统浅色；浅色下 Material3 `TopAppBar` 用 `surface=Color.White` → 四个 Tab 标题栏一片白；再加 `enableEdgeToEdge()` 后主题又把 `statusBarColor` 设成不透明 primary，出现色差。
- **白边修复**：`theme/Theme.kt` 改为 `darkTheme = true`（强制深色）；SideEffect 里 `statusBarColor`/`navigationBarColor` 设 `Color.Transparent`，由应用栏颜色接管，消除白边与色差；移除未用的 `isSystemInDarkTheme` import。
- **离线弹错误根因**：`DashboardScreen` 用 `when { isLoading->转圈; error!=null->全屏“无法连接”覆盖层 }`，离线连不上最终变 ERROR，整页被挡，没法用。
- **离线修复**：删除该 `when` 阻塞分支，仪表盘始终渲染；顶部 `StatusIndicator` 按真实状态显示 已连接/连接中/未连接；新增非阻塞横幅：RECONNECTING（tertiaryContainer）与 ERROR（errorContainer，文案“未连接咖啡机——仍可离线查看萃取记录与档案”，带“配置连接”按钮）。`isLoading`/`error` 局部变量已删。历史/档案页本就读本地 Room（`ShotHistoryViewModel.loadShots()` 先 `localRepo.getAllShots()`，后台 sync 失败静默），离线可用。
- 新增字符串 `dashboard_disconnected`、`dashboard_offline_hint`（en+zh）。
- `assembleDebug` 通过（仅若干改动前就存在的 Elvis warning）。

## 改动：横屏图表只显示 1/3 高度（session 4 续）
- 根因：全屏图表 `history_chart/{shotId}` 和 `livecurve` 仍被包在主 Scaffold 的**底部 Tab 导航栏**里，横屏时那条 NavigationBar 吃掉一大块高度，加上 TopAppBar + 系统手势条，图表被压成窄条。`ShotChartFullScreen` 内部 `weight(1f)` 布局本身没问题。
- 修复：`AppNavigation.kt` 的 `bottomBar` 改为仅当 `currentRoute` 属于 4 个 Tab 目的地时才显示 NavigationBar；全屏目的地隐藏底部栏，图表自动撑满腾出的高度。
- `assembleDebug` 通过。

## 改动：About 加打包时间（session 4）
- `app/build.gradle.kts`：`defaultConfig` 注入 `buildConfigField("String","BUILD_TIME",...)`，时间戳在配置阶段用 `SimpleDateFormat("yyyy-MM-dd HH:mm:ss", Locale.US)` 生成 → 每次 `assembleDebug` 自动刷新（已加顶部 `import java.text/Date/Locale`）。
- `SettingsScreen.kt` About 区在 version 下新增一行 `BuildConfig.BUILD_TIME`，标签走 `settings_build_time`（en/zh 各加一条，中文"打包时间：%1$s"）。
- 验证：`assembleDebug` 通过，生成 `BuildConfig.java` 内 `BUILD_TIME="2026-07-20 15:34:59"`。

## 用户约定（session 4 起生效）
- 每次改完代码都必须跑 `./gradlew :app:assembleDebug` 打包验证，BUILD SUCCESSFUL 才算完成。
- 非代码内容用中文回复。已写入 MEMORY.md。

## Fixes applied (session 3): brew UX — DONE, assembleDebug passes
- **User instruction**: output non-code content in Chinese (中文) from now on.

## 改动：三次失败停自动重连 + 手动重试（session 4 续3）
- 用户要求：三次连不上就别自动连了，改手动触发，不然一直弹。
- `MachineSessionManager.kt`：`MAX_RECONNECT_ATTEMPTS` 6→3（恰好 1 次初始+2 次重试，共 3 次连接失败后停）；`scheduleReconnect` 改为进入即 `reconnectAttempt++`，达到上限设 `ConnectionState.ERROR` 并 return（不再递归排重连）；新增 `retryConnection()` 公开方法：取消重连 job、清零计数、`webSocket?.cancel()`、设 CONNECTING 后 `connect()`。
- `DashboardScreen.kt`：ERROR 横幅新增"重试连接"按钮（Refresh 图标 + `dashboard_retry` 文案），`onClick = { session.retryConnection() }`，保留"配置连接"。
- 字符串：en + zh 各加 `dashboard_retry`（中文"重试连接"）。
- `assembleDebug` 通过（仅既有 Elvis/未用变量 warning）。

## 改动：横屏页顶栏小字 + Profile 独立详情页 + 空数据埋点（session 4 续4）
- **任务1 横屏页顶栏**：`ShotChartFullScreen.kt` 与 `LiveCurveScreen.kt` 去掉 `TopAppBar`（高空白条），改为一行小字 Row（名称/时长/关闭 或 返回/标题/状态 chip），不再用 Title 控件。
- **任务2 Profile 独立详情页**：新增 `ui/profiles/ProfileDetailScreen.kt`（`profile_detail/{profileId}` 导航目的地），上半部分 `BrewChartView` 图表、下半部分 `LazyColumn` 数据（作者/备注/阶段卡片）。原 `ProfilesScreen` 弹窗 `ProfileDetailDialog` 删除，卡片 onClick 改为 `navController.navigate("profile_detail/$id")`；`generateProfileChartPoints`/`PhaseCard`/`toPhaseV3`/`ProfileEditDialog` 迁移到 `ProfileDetailScreen.kt`（包级可见，列表的编辑弹窗复用）。`AppNavigation` 注册路由；`profile_detail` 路由不在底部 Tab 列表 → 底部栏自动隐藏。
- **任务3 Profile 空数据埋点**：全链路加 `Log.d/w/e`，统一 TAG `GagMateProfile`（受 `BuildConfig.DEBUG` 保护）：①`SyncManager.fullSync` 打印机器返回的 profile 数/每个 id+name、sendGetProfile 失败；②`MachineSessionManager.sendGetProfile` 打印帧 hex；③`parseProtoMessage`(d_prof/d_act_prof) 打印 name/phases/payload 长度；④`handleMessage` ActiveProfileMsg 打印 name/phases，0 phases 告警；⑤`handleJsonMessage` 新增 else 分支打印未处理的 action（抓"profile 以 JSON 文本帧到达被丢弃"的情况）；⑥`ProfileRepository` WS→Room 收集器打印收到的 name/phases、本地所有 profile 名、是否匹配、是否写入；⑦`ProfileDetailScreen` 打印打开 profileId、name/machineProfileId/phasesJson 长度/phasesFromJson 数/hasNoPhases、是否发起 g_prof。
- 加字符串 `profile_phases`/`profile_no_phases`/`profile_fetching_phases`（en+zh）。
- `assembleDebug` 通过（编译中修掉两处：ProfileDetailScreen 缺 `items` 导入、KeyboardType 重复导入；该文件曾被外部进程删过一次，已重建）。
- **初步判断**：机器同步只存 id/name，`phasesJson` 留空，依赖 `g_prof`→`d_prof` 的 WS 回包按 name 回填 Room。空数据最可能是：(a) `d_prof` 以 JSON 文本帧到达但 `handleJsonMessage` 只处理 sensor/shot → 被静默丢弃（新 else 日志可证实）；(b) 回包 name 与本地存储 name 不完全一致 → 匹配失败（收集器日志会打 localNames 与 matched）。等用户给日志再定位。
- Removed Dashboard "Start Brew" FAB (machine is the only thing that can start a brew; dashboard keeps status display via `MachineStatusBadge` on `brewActive`).
- **Critical architecture fix**: moved `shotSnapshot`→`chartData` buffering OUT of `DashboardViewModel` into `ShotRepository.start(scope)`, launched from new `AppContainer.appScope` (process-lifetime `CoroutineScope`). Previously the buffer died when navigating away from Dashboard → live curve froze. `appendShotPoint` now also captures temperature + weight (was pressure/flow only).
- Added `LiveCurveScreen` (landscape, full-screen `BrewChartView`, live P/PF/T/W readouts, back button). `BrewChartView` gained `height = Dp.Unspecified` to fill parent.
- Auto-jump to curve mode: `AppNavigation` watches `brewActive` rising edge while on Dashboard route → `navigate("livecurve")`. Uses `navController.currentDestination` (not captured route) so returning from LiveCurve doesn't re-trigger.
- Rewrote `ShotChartFullScreen` as landscape NavHost destination `history_chart/{shotId}` (loaded via `produceState` + `localRepo.getShotById`). Now has real left/right axes with labels, time axis auto-fit to shot duration, **drag-to-scrub** crosshair with live readout, and zoom (1×–20×) + pan slider. Replaced the old tap-only overlay (which couldn't cover the bottom nav bar).
- `ShotHistoryScreen` now navigates via `onOpenChart(id)`; removed the overlay + unused `ShotChartFullScreen(shot=...)` signature.
- New strings added (en + zh): live_curve_title/brewing/idle/waiting, history_chart_title/no_data/zoom_in/zoom_out/hint.
- All documented in CODE_REVIEW.md §0b. Build: `./gradlew :app:assembleDebug --offline` green.

## 改动：仪表盘全新设计系统重构（session 5，UI Designer 角色）
- 新增 `theme/Tokens.kt`：设计 token 单一来源 —— 间距(GagMateSpacing 4/8/12/16/24/32)、形状(GagMateShape sm/md/lg/xl/pill)、海拔(GagMateElevation)、语义色(GagMateExtendedColors：success/warning/info/state{Brewing,Heating,Idle,Steam}/gauge{Track,Temperature,Pressure,Flow,Steam}/divider/disabled，明暗双套，均达 WCAG AA 对比度)。
- `theme/Theme.kt` 用 `CompositionLocalProvider(LocalGagMateColors …)` 注入扩展色；组件内用 `gagMateColors()` 取用，不再在组件里硬编码 `Color(0xFF…)`。
- 组件库 token 化 + 无障碍：`StatusIndicator`（连接脉冲点取 success/disabled）、`MachineStatusBadge`（本地化标签 Brewing/Heating/Idle/Steam + 合并 contentDescription）、`GaugeView`（轨道/填充取 token，合并 contentDescription 供读屏）。
- 重写 `ui/dashboard/DashboardScreen.kt`：移除泄漏给用户的 `DBG:` 调试文本；Hero 状态卡（曲线名 + 状态徽章 + 设定温度 + 运行时长）、机器控制卡（Flush/Tare 48dp 触控、设定温度/重量步进器）、实时曲线卡（仅 chartData 非空时）、2×2 仪表、萃取中卡（体积显示实时称重）、机器状态卡；连接中显示骨架屏，重连/离线为非阻塞顶部横幅；`BoxWithConstraints` 在宽屏(>720dp)居中限宽 720dp。
- 新增字符串（en+zh）：dashboard_no_profile/active_profile/setpoint_short/hero_uptime/weight/status_{brewing,heating,idle,steam}。
- 无障碍：仪表/徽章/指标块合并 contentDescription；主按钮与步进器 `defaultMinSize(minHeight=48.dp)`。
- 构建：`assembleDebug --offline` 通过（clean build 亦通过）。注意：工作区有外部进程会自动增删/改文件（如 strings.xml 被改、ProfileDetailScreen.kt 曾被删又重建），打包若短暂报 clash/未解析引用，多半是该进程中间态，稍后重跑即过。
- 交付文档：`GagMate_DesignSystem.md`（设计系统 + 仪表盘改版 handoff）。

## 改动：四个主 Tab 顶栏也去掉高空白 TopAppBar（session 4 续5）
- 用户反馈"所有 title 的空白高度还没解决"——横屏图表页上一轮已改小字头，但四个主 Tab（Dashboard/Profiles/History/Settings）仍用默认 `TopAppBar`（默认最小高度大 + 内部留白多），所以顶部空白条还在。
- 新增 `ui/components/CompactTopBar.kt`：紧凑一行小字头（无 Title 控件），`Row` + `statusBarsPadding()` + `padding(horizontal=14dp, vertical=8dp)`，左可选 navigationIcon、中 title 槽、右 actions。
- 四个主 Tab 的 `Scaffold topBar` 全部从 `TopAppBar` 换成 `CompactTopBar`：标题改 `titleMedium + Bold`，保留原刷新/设置/导入/返回等图标按钮；Dashboard 的连接状态 `StatusIndicator` 内联在标题行。
- 编译报错：`SettingsScreen` 用 `FontWeight.Bold` 但缺 `import androidx.compose.ui.text.font.FontWeight`（其余三页本来就有），补导入后 `assembleDebug` 通过（仅既有未用变量 warning）。
- 注意：这些文件用 `import androidx.compose.material3.*` 通配，`TopAppBar`/`TopAppBarDefaults` 去除后无需改 import。

## 改动：横屏图表 hover 小窗补 WR + tap 定位（session 4 续6）
- 用户反馈：横屏图表页 hover 小窗漏了 WR g/s（吸取率）；且小窗只有滑动触摸才出现/更新。
- `ShotChartFullScreen.kt`：①Readout 那排补 `if (hasWRate) Readout("WR", "%.1fg/s".format(closest.weight), WRATE_COLOR)`（注意命名：绿线 `it.weight`=weight rate g/s，棕线 `shotWeight`=累计重量 g）。②新增 `detectTapGestures(onPress/onTap)` 的 pointerInput（与原 `detectDragGestures` 并存两个 pointerInput 块），单击/按下即可定位 crosshair，不必先滑动。新增 import `detectTapGestures`。
- `assembleDebug` 通过。

## 改动：Profile 详情内联编辑 + 先推机器再落库（session 4 续7）
- 用户要求：曲线详情点编辑后，编辑控件直接显示在图表下方（不要弹窗）；数值编辑要立刻更新图表；点保存才推机器，机器确认成功才落库，不保存就不动。
- `ProfileDetailScreen.kt`：删除旧的 `ProfileEditDialog`，改为内联 `ProfileEditPanel`（图表下方）。`ProfileDetailContent` 维护 `isEditing + editedName/Author/Notes/Phases` 本地副本；图表 `chartPhases = if (isEditing) editedPhases else phasesList`，`generateProfileChartPoints` 用 `remember(chartPhases)`，编辑阶段数值即重算（实时更新）。头部在编辑态显示「保存 / 取消」：保存→`doSave()`（scope.launch 调 `onSave`）；取消→`discardEdits()` 还原。保存中显示转圈、失败顶部 error 横幅。`onSave` 类型为 `suspend (ProfileEntity) -> Result<Unit>`。
- `ProfileRepository.kt`：新增 `pushAndSaveIfConfirmed(entity)`——`machineRepo.uploadProfile(entity.toShotProfile()).getOrThrow()`（HTTP 200=机器确认），**只有成功后才** `localRepo.saveProfile(...)` 写 Room；失败/未确认→抛异常→本地不写。旧 `saveEditedProfile`（先落库再上传）保留但已无调用方。
- `ProfilesViewModel.kt`：新增 `suspend fun pushAndSaveIfConfirmed(...)` 委托到 repo。

## 改动：曲线组件抽取 + 萃取唤醒 + 阶段解析（基于 gagmate_combined(2).log 四个问题）
- **需求（用户日志）**：①萃取开始 App 不唤醒实时曲线；②很多 profile 阶段数正确但内容全 0；③曲线绘制应支持变化模式（EASE_OUT/EASE_IN_OUT…）；④曲线图抽出共用组件替换单独实现。
- **① brewActive 来源修复**：`_brewActive` 原只从 JSON `sensor_data_update.brewActive` 置位，protobuf 固件（只发 `ShotSnapshot`/`SystemState`）永不触发 → 萃取不唤醒。改为 OR 两信号 `brewActiveFromJson` + `brewActiveFromProtobuf`（`ShotSnapshot.timeInShot>0`）；`launchBrewWatchdog()`（4s）兜底：超时无 in-shot 快照则清零（机器可能不发结束帧）。`start()` 启看门狗、`stop()` 取消。
- **① 导航放松**：`AppNavigation` 自动跳转不再要求"当前在仪表盘"，改为从任意 Tab 跳转（仅跳过已处全屏图表 livecurve/history_chart/* 时）。
- **② 阶段全 0 根因**：`ProtoDecoder.decodePhaseInfo()` 误把 phase 的 **名称 field 2** 当嵌套 Target 解析，从未读 start/end/curve/time。`BrewPhase` 增 `start`/`variation`（`PhaseV3.toBrewPhase()` 携带）；`decodePhaseInfo` 改从 **field 3 嵌套 Target** 读 sub1=start/sub2=end/sub3=curve 枚举(新增 `curveEnumToString` 0–6)/sub4=time(ms→s)；`parseProfilePhases()` 重建 `BrewPhase`。
- **③ 变化模式**：`CurveChart.kt` 实现 `curveVariationEasedProgress(variation,p)`（FLAT/LINEAR、EASE_IN t²、EASE_OUT 1−√(1−t)、EASE_IN_OUT t²(3−2t)、FAST_IN √t、FAST_OUT 1−(1−t)²、FAST_IN_OUT smoothstep）；`generateProfileChartPoints()` 按阶段 start→target 缓动生成，且上一阶段末端值作本阶段起点保证连续。修复原 FLAT-only。
- **④ 共用组件**：新增 `ui/components/CurveChart.kt`（`CurveSeries(color,axis,dashed=false,valueAt)`、`enum ChartAxis{LEFT,RIGHT}`、`@Composable CurveChart(...)` 网格/虚线target/实线actual/末端标记/十字线/窗口 t0..t1、`ProfileCurveChart(phases)` 封装）。`BrewChartView` 与 `ShotChartFullScreen` 删除各自重复 Canvas，改构造 `List<CurveSeries>` 委托 `CurveChart`（保留各自交互：缩放/平移/擦洗/十字线、图例、坐标轴标签）。移除 `ProfileDetailScreen` 里 FLAT-only 本地 `generateProfileChartPoints`，`ProfileDetailScreen` 与 `DashboardScreen` 统一用 `CurveChart.kt` 共享实现（`DashboardScreen.ProfileGlobalsCard` 当前曲线卡即受益）。
- **踩坑**：`CurveSeries` 构造参数原序 `(color,axis,valueAt,dashed=false)`，尾随 lambda 被绑到 `dashed`(Boolean) → 编译错；改为 `valueAt` 末位。另 `ShotChartFullScreen` 缺 `onSizeChanged` 导入、`CurveChart` 缺 `remember` 导入，均已补。
- 文档同步：README（实时曲线自动打开 + 文件树加 CurveChart.kt）、GAGMATE_REFERENCE.md（CurveChart 组件表+§7.2、brewActive 注释、Profile protobuf 阶段结构修正）、GagMate_DesignSystem.md（组件表加 CurveChart + 当前曲线卡说明）、CODE_REVIEW.md（新增 §0c 记录四项修复）。
- 构建：`/gradlew :app:assembleDebug --offline` BUILD SUCCESSFUL（仅产线既有 Elvis/未用变量 warning）。
- `ProfilesScreen.kt`：列表卡片 `onEdit` 改为 `onOpenProfile(id)`（直接进详情页，详情页内点编辑）；删除旧的 `showEditDialog`/`editingProfile` 状态与 `ProfileEditDialog` 调用块。
- 字符串：en/zh 各加 `profile_saving`（保存中…）、`profile_saved_hint`（已保存到咖啡机 / Saved to machine）。
- 编译报错修复：`vm.pushAndSaveIfConfirmed` 先在 VM 加委托方法；`doSave` 内 `stringResource` 移到 Composable 作用域（预存 `saveFailedText`）；`ProfileEditPanel` 补 `@OptIn(ExperimentalMaterial3Api::class)`（用了 FilterChip）。
- `assembleDebug` 通过（仅既有未用变量/非空断言 warning）。

## 17:00 UI 调整（用户截图反馈）
用户反馈：
1. title 上方空白仍过高；
2. 未连接提示应嵌入页面而非大浮窗；
3. 重连时不要闪空白页，用页面内状态行；
4. 仪表盘设定温度/目标液重应取当前 profile，而不是机器实时 sensor targetTemperature。

修改：
- `CompactTopBar.kt`：`vertical = 8.dp` → `0.dp`，让标题行更贴顶，减少空白。
- `ProtoCommands.kt`：新增 `readProfileTemperature(payload)` 和 `readProfileTargetWeight(payload)`，从 profile protobuf 解析设定温度（field 4）和目标液重（field 3 inner field 2）。
- `MachineSessionManager.kt`：新增 `_activeProfileSetpointTemp` / `_activeProfileTargetWeight` StateFlow；收到 `ActiveProfileMsg` 时解析并更新；`updateActiveProfileTemperature/Weight` 成功后同步更新 StateFlow。
- `DashboardScreen.kt`：
  - 用 `session.activeProfileSetpointTemp` / `activeProfileTargetWeight` 替换 sensor 的 `targetTemperature` 作为设定温度/目标液重显示。
  - 把连接状态从浮层 Surface 改为 `LazyColumn` 内部的 `InlineConnectionStatus` Card（未连接/重连中均内嵌页面，不再遮挡内容）。
  - 删除 `CONNECTING` 时的 `DashboardLoadingSkeleton` 全屏骨架屏，避免重连闪空白。
  - `ControlCard` 的"重量"显示从 `liveWeight` 改为 `targetWeight`。
  - 删除未用 import/变量。
- `assembleDebug` 通过，无 warning。

## 首页展示 active profile + 全局设定参数
- 根因：①`parseProfileDict` 漏解析 `selected` 字段（proto field 3），导致 `ProfileRef.isSelected` 恒 false，`selectedProfileName` 永远空 → Hero 卡一直"No profile"。②`ActiveProfileMsg`(d_act_prof/d_prof) 的 `msg.name` 没有回写到 `selectedProfileName`。③连接后没主动拉 active profile 完整数据，完全靠机器推送。
- 修复：
  - `ProtoDecoder.parseProfileDict` 解析 field 3 (selected bool)。
  - `ProtoCommands` 新增 `ProfileGlobals` data class + `extractProfileGlobals(payload)`：解析 field4 水温 + field3 内 field2/3/4/5（目标重量/压力/流量/时间）。
  - `MachineSessionManager`：新增 `_activeProfileTargetPressure/Flow/Time` StateFlow；`ActiveProfileMsg` 处理里 `selectedProfileName = msg.name` 并填全部 globals；`ProfileDictMsg` 到达时 `sendGetProfile(selected.id)` 主动拉取选中 profile 完整数据（即使机器不推 d_act_prof 也能拿到 phases+globals）。
  - `DashboardScreen`：收集 3 个新 flow；新增 `ProfileGlobalsCard`（设定温度/目标液重/目标压力/目标流量/目标时间，0 值显示"—"）；新增 `fmt()` helper。
  - 字符串：dashboard_profile_globals / dashboard_target_pressure / dashboard_target_flow / dashboard_target_time（中英文）。
- 用户待确认：d_act_prof 是否能正常到达（之前 profile 详情数据全空仍未定位）——已在 ActiveProfileMsg/ProfileDict 加 GagMateProfile 日志，等用户给日志。

## 彻底干掉官方 title 控件，标题内联进页面
- 用户反馈：CompactTopBar 虽是自定义组件，但仍作 Scaffold 的 `topBar` 独立槽位渲染，顶部 `statusBarsPadding` 撑出高空白；要求连官方 title 控件也不要，把标题文字+按钮做到页面内容里。
- 新增 `ui/components/PageHeader.kt`：纯 `Row` 组合（非 TopAppBar、非 topBar 槽位），只加 `padding(vertical=8.dp)`；状态栏避让交由容器 `statusBarsPadding()`。
- 四个主页（Dashboard/Profiles/History/Settings）均：删除 `topBar={CompactTopBar(...)}`，改为把 `PageHeader` 作为 `LazyColumn`/`scrollable Column` 的第一个 item；`LazyColumn` 加 `statusBarsPadding()`、contentPadding 改为仅 `horizontal`；连接状态行/transient Snackbar 保持内联。
- 删除 `CompactTopBar.kt`（不再被引用，grep 已确认无残留 import）。
- 编译：首编因 Dashboard 末尾多一个 `}`（旧外层 Box 的闭合）报错 `Expecting a top level declaration`，删掉多余 `        }` 后重编 BUILD SUCCESSFUL。仅既有未用变量 warning。
- `assembleDebug` 通过，APK 已产出。

## 曲线(profiles) tab 去掉 loading，本地数据即开即显 + 后台刷新 + toast
- 用户反馈：一点开"曲线"tab 就全屏 loading 转圈，要求直接显示本地数据、后台更新、数据回来弹 toast。
- 根因：`ProfilesScreen` 的 `when` 用 `isLoading` 分支显示全屏 `CircularProgressIndicator` 替代列表；`loadProfiles()` 一打开就 `_isLoading=true` 直到同步完成；`syncMessage`（"数据已更新"信号）被 collect 却从未展示（旧 LaunchedEffect 只 3s 后清空，无可见反馈）。
- 修复：
  - `ProfilesViewModel.loadProfiles()`：不再置 `_isLoading`，改为 `_isSyncing=true`（后台同步指示），本地数据靠 `init{}` 里的 `profilesFlow` 立即展示。
  - `ProfilesScreen`：删除 `isLoading` 分支与 collect；`when` 改为 `profiles.isEmpty() && !isSyncing -> 空状态`，否则直接渲染列表（含 header + 卡片）。
  - 头部 actions 加 `if(isSyncing) CircularProgressIndicator(size=20dp)` 做不阻塞的同步中指示。
  - `syncMessage` 改为 `LaunchedEffect` 中 `Toast.makeText(...).show()`（数据回来弹 toast），随后 `clearSyncMessage()`。
  - 新增 `import android.widget.Toast`。
- `assembleDebug` BUILD SUCCESSFUL。仅既有未用变量 warning（pendingUploadCount / createSampleProfile 内 entity）。

## 仪表盘去掉 PageHeader，按钮并入 Hero 卡
- 用户截图反馈：仪表顶部仍有"留空"。根因是仪表盘仍保留一个独立的 `PageHeader` 行（标题 + 状态 + 操作按钮），形成与卡片分离的标题条/空白感。
- 修复：
  - 删除 `DashboardScreen` 的 `PageHeader` item 与 `StatusIndicator` import。
  - `HeroStatusCard` 新增 `onRefresh`/`onOpenSettings` 参数，在卡片头部右侧加入刷新/设置两个 `IconButton`。
  - 页面标题由底部 Tab 栏承担；操作按钮真正进入页面内容（Hero 卡）。
  - `InlineConnectionStatus` 的垂直内边距从 `GagMateSpacing.sm` 减到 `GagMateSpacing.xs`，更紧凑。
- `assembleDebug` BUILD SUCCESSFUL。

## 消除页面标题下方的“留空”
- 用户截图：历史页顶部状态栏与“萃取历史”标题之间空了一大块（红圈）。
- 根因：历史/曲线/设置三个页面的 `LazyColumn`/`Column` 都加了 `.statusBarsPadding()`，把整个内容向下推了一个状态栏高度；再加上 `PageHeader` 自己的 `vertical = 8.dp`，标题离状态栏就空了。
- 修复：
  - `PageHeader`：Row 自己加 `.statusBarsPadding()`，并把 `padding(vertical=8.dp)` 改为只保留 `padding(bottom=8.dp)`；标题文本紧贴状态栏下沿。
  - 历史/曲线/设置：去掉 `LazyColumn`/`Column` 的 `.statusBarsPadding()`，把 `contentPadding` 只保留水平。
  - 设置页：去掉 Column 的 `statusBarsPadding()`，并把 `padding(horizontal=16.dp, vertical=8.dp)` 改为 `padding(horizontal=16.dp)`。
  - 仪表盘：没有 `PageHeader`，但 `LazyColumn` 同样加了 `.statusBarsPadding()`；去掉后改为让第一个可见项自己处理：`InlineConnectionStatus` 和 `HeroStatusCard` 新增 `modifier` 参数，未连接时由 `InlineConnectionStatus` 加 `statusBarsPadding()`，已连接时由 `HeroStatusCard` 加。
- 编译：BUILD SUCCESSFUL；仅剩既有 `pendingUploadCount` 未使用 warning。

## 仪表盘交互精简 + 历史日期修复
- 未连接提示条（`InlineConnectionStatus`）：文案 `dashboard_offline_hint` 改为仅“未连接”；去掉“重试连接/配置连接”两个 TextButton（页面 Hero 卡里已有刷新/设置按钮）。`onRetry`/`onConfigure` 参数一并移除，调用处同步更新。
- Hero 卡 profile 名超长：名字 Column 加 `weight(1f)` + `maxLines=2` + `TextOverflow.Ellipsis`，徽章和按钮不再被挤掉。
- 机器控制块：删除权重行里冗余的 “T” 步进按钮（与 TARE 主按钮重复）；重量标签 `dashboard_weight` 改为“设定液量”。
- 删除底部“机器状态”冗余卡（只显示“今日萃取 --”和已重复展示的设定温度）。
- 历史日期 bug：显示处原 `Date(shot.timestamp * 1000)` 与 `toShotRecord()` 的 `timestamp*1000L` 双重换算（毫秒再 ×1000）；改为 `normalizeShotTs()` 兼容库中混存的秒/毫秒时间戳（>1e12 视为毫秒，否则 ×1000），统一按毫秒显示。
- 编译：首编漏 `TextOverflow` import 报错，补 `import androidx.compose.ui.text.style.TextOverflow` 后 BUILD SUCCESSFUL。

## 文档更新 + 根级规则 (晚)
- 新建根目录 `AGENTS.md`：Rule 1 = 所有修改必须同步更新文档（README / GAGMATE_REFERENCE / GagMate_DesignSystem / CODE_REVIEW），附工程约定速记（先打包再汇报、禁硬编码色、双语 strings）。
- `GAGMATE_REFERENCE.md` §9：DB 升到 v4，新增 MIGRATION_3_4 + 时间戳 canonical(ms) 约定（TimeUtils.normalizeShotTimestamp）；附录 B 新增第 7 条（double ×1000 已修复）。
- `GagMate_DesignSystem.md`：组件表更新 MachineStatusBadge(offline/connecting/reconnecting)+GaugeView.showDash；§4 重写仪表盘布局（移除 TopAppBar/机器状态块、连接状态并入 badge、无数据显示 —、新增当前曲线卡片）；颜色表加 stateOffline；新增 §8 迭代记录。
- assembleDebug 复验 BUILD SUCCESSFUL（曲线图表改动）。
